# Step 01: Setup Quasar Project

In project root directory

```bash
$ quasar --version
1.2.1
$ quasar create ui
...
? Project name (internal usage for dev) simple-chat-ui
? Project product name (must start with letter if building mobile apps) Simple Chat
? Project description Simple Chat Application
? Author Johan Coppens <johan.coppens@edugolo.be>
? Pick your CSS preprocessor: SCSS
? Check the features needed for your project: ESLint (recommended)
? Pick an ESLint preset: Prettier
? Continue to install project dependencies after the project has been created? (recommended) NPM
...
To get started:

  cd ui
  quasar dev
...
$ cd ui
$ quasar dev
...
App • Opening default browser at http://localhost:8081

```
